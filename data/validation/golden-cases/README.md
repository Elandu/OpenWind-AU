# Reviewed golden cases

Place independently calculated engineering reference cases in this directory.

Every case must conform to `../golden-case.schema.json` and must identify a real independent
reviewer and review date. Do not add placeholder reviewer identities or generate expected values
from the production OpenWind-AU calculation path.

Licensed standards text must not be committed. Record only the source references and derived
engineering values needed for reproducible validation.
